# =============================================================================
# RA-D-PS Database Configuration
# =============================================================================

# Database Backend Selection
# Set to 'postgresql' for local development or 'supabase' for cloud deployment
DATABASE_BACKEND=postgresql

# =============================================================================
# Local PostgreSQL Configuration (when DATABASE_BACKEND=postgresql)
# =============================================================================

# PostgreSQL Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ra_d_ps
DB_USER=ra_d_ps_user
DB_PASSWORD=your_secure_password_here

# Connection Pool Settings
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# SSL/TLS Settings (optional)
DB_SSL_MODE=prefer
# DB_SSL_CERT=/path/to/client-cert.pem
# DB_SSL_KEY=/path/to/client-key.pem
# DB_SSL_ROOT_CERT=/path/to/server-ca.pem

# Debug Settings
DB_ECHO_SQL=false
DB_POOL_PRE_PING=true

# =============================================================================
# Supabase Cloud Configuration (when DATABASE_BACKEND=supabase)
# =============================================================================

# Supabase Project Details
# Get these from your Supabase project dashboard at https://app.supabase.com
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here_optional

# Supabase PostgreSQL Connection
# Database password from Supabase project settings -> Database
SUPABASE_DB_PASSWORD=your_database_password_here
# Optional overrides (auto-detected from SUPABASE_URL by default)
# SUPABASE_DB_HOST=db.your-project-ref.supabase.co
# SUPABASE_DB_PORT=5432
# SUPABASE_DB_NAME=postgres
# SUPABASE_DB_USER=postgres

# Supabase Connection Pool (cloud-optimized defaults)
# SUPABASE_POOL_SIZE=3
# SUPABASE_MAX_OVERFLOW=5
# SUPABASE_POOL_TIMEOUT=30
# SUPABASE_POOL_RECYCLE=1800

# Supabase Debug
# SUPABASE_ECHO_SQL=false

# =============================================================================
# Application Settings
# =============================================================================

ENVIRONMENT=development
LOG_LEVEL=INFO

# =============================================================================
# Quick Setup Guide
# =============================================================================
#
# For LOCAL DEVELOPMENT:
#   1. Set DATABASE_BACKEND=postgresql
#   2. Configure DB_HOST, DB_USER, DB_PASSWORD above
#   3. Start PostgreSQL: docker-compose up postgres
#   4. Run migrations: python scripts/setup_database.py
#
# For SUPABASE CLOUD:
#   1. Create Supabase project at https://app.supabase.com
#   2. Set DATABASE_BACKEND=supabase
#   3. Copy SUPABASE_URL and SUPABASE_KEY from project settings
#   4. Copy database password from Database settings
#   5. Run migrations: python scripts/setup_supabase.py
#   6. View data in Supabase dashboard: https://app.supabase.com/project/_/editor
#
# SWITCHING BACKENDS:
#   - Change DATABASE_BACKEND value
#   - Restart your application
#   - No code changes required!
#
# =============================================================================
